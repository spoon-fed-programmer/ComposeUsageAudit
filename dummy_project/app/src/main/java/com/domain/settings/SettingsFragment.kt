package com.domain.settings

import com.common.compose.button.PrimaryButton

class SettingsFragment {
    fun render() {
        PrimaryButton() // 1 reference
        
        // Comments that should not be counted:
        // PrimaryButton()
        
        /*
        PrimaryButton()
        */
    }
}
